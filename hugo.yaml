baseURL: "/"
languageCode: "en-us"
title: "Pathfinder - Kingmaker"
summaryLength: 30
disableKinds:
  - taxonomy
  - term
markup:
  highlight:
    noClasses: false
  goldmark:
    renderer:
      unsafe: true
module:
  hugoVersion:
    extended: true
    min: 0.100.0

  mounts:
    - source: static
      target: static
    - source: layouts
      target: layouts
    - source: assets
      target: assets
